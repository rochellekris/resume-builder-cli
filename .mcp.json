{
    "mcpServers": {
        "resume-builder": {
            "command": "node",
            "args": [
                "dist/mcp/server.js"
            ],
            "cwd": "${workspaceFolder}"
        }
    }
}